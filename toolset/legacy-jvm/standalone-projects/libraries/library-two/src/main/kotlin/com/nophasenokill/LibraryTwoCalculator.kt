package com.nophasenokill

object LibraryTwoCalculator {
    fun multiply(a: Int, b: Int): Int {
        // return b * a
        return a * b // Uncomment line above and comment this line out to see what changes buildAll has
    }
}
